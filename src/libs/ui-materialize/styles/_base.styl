

// code
//     background  #e5e5e5
//     border 1px solid lightgrey
//     padding 20px

.promo-caption
    font-size: 1.7rem;
    font-weight: 500;
    margin-top: 5px;
    margin-bottom: 0px;

max-out()
    // max-width 100vw
    // min-width 100vw
    // max-height 100vh
    // min-height 100vh
    // width 100vw
    // height 100vh


html
    font-size none
    &.has-flex, .has-flex
        box-sizing(border-box)

        // max-out()
        overflow-x hidden
        min-width 100vw
        max-width 100vw
        width 100%
        position relative
        // font-size 0

        height 100%
        min-height 100%
        max-height 100%
        flex-container()

        *
            box-sizing(border-box)


body
    &.has-flex

        min-width 100vw
        max-width 100vw
        min-height 100vh
        max-height 100vh
        position relative
        overflow-x hidden
        overflow-y scroll
        flex-container()
        flex--direction(column)
        // box-sizing(border-box)
        // max-out()
        // overflow-x hidden

// .mz-materialize
//     width 100%
//     flex 1 0 auto
//     min-width 100%


.mz-materialize
    font-size 15px
    height 100%
    flex-container()
    flex-direction row
    margin 0
    min-width 100vw
    max-width 100vw
    width 100vw
    z-index 10
    // -webkit-overflow-scrolling: touch
    // overflow-y: scroll

    // scroll-vertical()

    &.mz-flex-column
        flex-direction: row

    .mz-nav
        flex-container()
        box-sizing(border-box)
        flex: 1 0 auto
        flex-direction: row
        width: 100%
        height $nav-top-height

    .mz-main
        box-sizing(border-box)
        // flex: 1 0 auto
        // flex-container()
        // flex-direction: row

    .mz-view
        // box-sizing(border-box)
        // flex-container()
        // flex: 1 0 auto

    .mz-body
        box-sizing(border-box)
        // 2scroll-vertical()
        flex: 1 0 auto
        flex-direction column
        transition none !important
        // overflow hidden

        height 100%
        min-height 100%
        max-height 100%

    footer
        // box-sizing(border-box)
        // flex: 1 0 auto

.mz-side-nav-toggle {

    i {
        font-size: 2rem
    }
}



.bg-container
    position fixed
    z-index 1

    min-width 100vw
    max-width 100vw
    width 100vw
    min-height 100vw
    max-height 100vw
    height 100vw

.shape-1
    width 900px
    height 500px
    position fixed
    transform: rotate(142deg) scale(2) skewx(-20deg) translate(300px)
    opacity 0.7


