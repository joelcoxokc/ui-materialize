;(function() { 'use strict';

    angular
        .module('directives')
        .filter('directivesFilter', directivesFilter)
        ;

    /* @ngInject */
    function directivesFilter() {}

  }).call(this);
