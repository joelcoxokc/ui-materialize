;(function() { 'use strict';

    angular
        .module('layout')
        .controller('layoutController', layoutController)
        ;

    /* @ngInject */
    function layoutController() { }

  }).call(this);
